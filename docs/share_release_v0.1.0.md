# itbound v0.1.0 Share Copy

## Core Message

- PyPI release is live: `itbound==0.1.0`
- Install: `python -m pip install itbound`
- GitHub: `https://github.com/yonghanjung/Information-Theretic-Bounds`

## Korean Short Post

`itbound v0.1.0`를 PyPI에 배포했습니다.  
정보이론 기반 인과 bounds를 실험/CLI로 바로 실행할 수 있습니다.  
설치: `python -m pip install itbound`  
Repo: `https://github.com/yonghanjung/Information-Theretic-Bounds`

## English Short Post

`itbound v0.1.0` is now on PyPI.  
You can run information-theoretic causal bounds directly from CLI/Python.  
Install: `python -m pip install itbound`  
Repo: `https://github.com/yonghanjung/Information-Theretic-Bounds`

## Korean Longer Post

`itbound v0.1.0`를 정식 공개했습니다.  
이 패키지는 unmeasured confounding 상황에서 information-theoretic causal bounds를 계산하기 위한 도구입니다.  
CLI와 Python API를 모두 제공하며, `example`, `quick`, `standard` 실행 경로를 바로 사용할 수 있습니다.

- Install: `python -m pip install itbound`
- Quick check: `itbound --help`
- Repo/Docs: `https://github.com/yonghanjung/Information-Theretic-Bounds`

## English Longer Post

`itbound v0.1.0` is officially released.  
The package provides information-theoretic causal bounds under unmeasured confounding, with both CLI and Python API workflows.

- Install: `python -m pip install itbound`
- Quick check: `itbound --help`
- Repo/Docs: `https://github.com/yonghanjung/Information-Theretic-Bounds`

## One-Liner for Email/DM

`itbound v0.1.0` is available on PyPI (`python -m pip install itbound`), with reproducible CLI and API workflows for information-theoretic causal bounds.
